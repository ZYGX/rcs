spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:rcs-register}:${NACOS_PORT:8848}
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml
  application:
    name: rcs